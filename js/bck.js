 $(document).ready(function(){
  /*
    var offset2 = $(document).height();
    //tambaño en pixels entre top y la imagen
    var lineHF = offset2 - $("#bottommark").position().top; 

    $(window).scroll(function(){
      var offset1 = $(document).height();
      var offset = $(window).scrollTop();
      var slide4 = $("#slide4").position().top;
      var slide5 = $("#slide5").position().top -600;
      var pixelsSlide4 = slide5 - slide4;
      var divisionSlide4 = pixelsSlide4/4;
      console.log(slide4, slide5, divisionSlide4*2, offset);

      var lineH = offset1 - $("#bottommark").position().top - offset;
      var lineHpart = lineHF/3;
        $("span").html(lineH);
            if (offset < divisionSlide4+slide4) {
                $("#animation").attr("src", "images/vap1.png");
            }
            if (offset < (divisionSlide4*2)+slide4 && offset > slide4) {
                  console.log('hola');
                $("#animation").attr("src", "images/vap2.png");
            }
            else {
                  console.log('hola');
                $("#animation").attr("src", "images/vap3.png");
            }
            // if ((offset <(divisionSlide4*2)+slide4) && (offset > divisionSlide4+slide4)) {
            //     $("#animation").attr("src", "images/vap2.png");
            // }
            // if (offset <(divisionSlide4*3)+slide4) {
            //     $("#animation").attr("src", "images/vap3.png");
            // }
        });
 */
    });

